# AWS Configuration
# Copy this file to .env.local for development
# For production, set these as environment variables or GitHub secrets

AWS_REGION=us-east-1

# AWS Credentials (for local development only - use IAM roles in production)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# DynamoDB Tables (from CDK output)
DYNAMODB_CONTENT_TABLE=wrs-content
DYNAMODB_ARTICLES_TABLE=wrs-articles
DYNAMODB_EVENTS_TABLE=wrs-events
DYNAMODB_GALLERY_TABLE=wrs-gallery
DYNAMODB_SUBSCRIBERS_TABLE=wrs-subscribers

# DynamoDB Local (development only)
# DYNAMODB_ENDPOINT=http://localhost:8000

# S3 Configuration (from CDK output: ImagesBucketName)
S3_BUCKET_NAME=wrs-images-YOUR_ACCOUNT_ID

# CloudFront (from CDK output: CloudFrontUrl)
CLOUDFRONT_URL=https://dXXXXXXXXXXXX.cloudfront.net

# Cognito Configuration (from CDK output)
NEXT_PUBLIC_COGNITO_USER_POOL_ID=eu-west-2_XXXXXXX
NEXT_PUBLIC_COGNITO_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX

# SES Configuration
SES_SENDER_EMAIL=noreply@whiterosespeakers.co.uk

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://whiterosespeakers.co.uk

# Development Mode (set to "true" for local dev without Cognito)
NEXT_PUBLIC_DEV_MODE=false

# ACM Certificate ARN (for custom domain - must be in us-east-1)
# ACM_CERTIFICATE_ARN=arn:aws:acm:us-east-1:ACCOUNT_ID:certificate/XXXXX
